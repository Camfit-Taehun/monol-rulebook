# 실전 워크플로우 예시

팀 환경에서 monol-rulebook을 효과적으로 사용하는 워크플로우를 소개합니다.

## 핵심 개념

```
개인 규칙 (글로벌)          프로젝트 규칙 (Git 공유)
~/.config/monol/rules/  +  ./rules/  →  최종 적용 규칙
        │                      │
        └──────────┬───────────┘
                   ↓
         Cursor에서도 적용
         Claude Code에서도 적용
```

- **글로벌 규칙**: 내 모든 프로젝트에 적용 (개인 스타일)
- **프로젝트 규칙**: 해당 프로젝트에만 적용 (팀 규칙, Git 공유)
- **도구 무관**: Cursor든 Claude Code든 같은 규칙 적용

## 목차

1. [새 프로젝트에 Rulebook 설정](#1-새-프로젝트에-rulebook-설정)
2. [팀원 온보딩 (규칙 공유)](#2-팀원-온보딩-규칙-공유)
3. [규칙 업데이트 및 배포](#3-규칙-업데이트-및-배포)
4. [개인 규칙과 프로젝트 규칙 분리](#4-개인-규칙과-프로젝트-규칙-분리)
5. [규칙 감사 및 정리](#5-규칙-감사-및-정리)

---

## 1. 새 프로젝트에 Rulebook 설정

### 시나리오

새로운 TypeScript 프로젝트를 시작하며 팀 코딩 표준을 적용하려고 합니다.

### 단계별 진행

#### Step 1: 기본 구조 생성

```
# 프로젝트 디렉토리 구조
my-project/
├── rules/
│   ├── .rulebook-config.yaml   # 설정 파일
│   ├── index.yaml              # 규칙 인덱스
│   ├── code/                   # 코드 규칙
│   └── workflow/               # 워크플로우 규칙
├── .cursorrules                # Cursor 규칙 (자동 생성)
└── .claude/rules/              # Claude 규칙 (자동 생성)
```

#### Step 2: 설정 파일 작성

`rules/.rulebook-config.yaml`:

```yaml
# Rulebook 설정
version: "1.0"

# 규칙 소스
sources:
  - "./code"
  - "./workflow"

# 상위 규칙 상속 (옵션)
inherit:
  - "~/.config/monol/rules"  # 글로벌 규칙

# 플랫폼 설정
platforms:
  cursor:
    enabled: true
    outputFile: ../.cursorrules
    format: markdown

  claude:
    enabled: true
    outputDir: ../.claude/rules
    format: mdc
    splitByCategory: true

# 기본값
defaults:
  severity: warning
  enabled: true
```

#### Step 3: 핵심 규칙 추가

```
/rule-add
```

**필수 규칙 예시:**

1. **TypeScript Strict 모드**
   ```
   이름: TypeScript Strict 모드
   카테고리: code/typescript
   심각도: error
   ```

2. **커밋 메시지 규칙**
   ```
   이름: Conventional Commits
   카테고리: workflow/git
   심각도: error
   ```

3. **코드 스타일**
   ```
   이름: 코드 포맷팅 규칙
   카테고리: code/style
   심각도: warning
   ```

#### Step 4: 플랫폼에 동기화

```
/rule-sync all
```

**출력:**

```
✅ 동기화 완료!

cursor: .cursorrules (3개 규칙)
claude: .claude/rules/ (3개 규칙)
```

#### Step 5: Git에 커밋

```
/commit -m "chore: add rulebook configuration"
```

---

## 2. 팀원 온보딩 (규칙 공유)

### 시나리오

새 팀원이 프로젝트에 참여했을 때 규칙을 빠르게 이해시키는 방법입니다.

### 단계별 진행

#### Step 1: 규칙 개요 확인

```
/rule --stats
```

**출력:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 규칙 통계
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

총 규칙 수: 12개

심각도별:
  🚫 error: 4개 (33%) - 반드시 준수
  ⚠️ warning: 6개 (50%) - 권장
  ℹ️ info: 2개 (17%) - 참고

카테고리별:
  code: 7개 (TypeScript, 스타일, 네이밍)
  workflow: 3개 (Git, PR)
  docs: 2개 (JSDoc, README)
```

#### Step 2: 중요 규칙 설명

```
/rule --severity error
```

**출력:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 필수 규칙 (error)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

이 규칙들은 반드시 준수해야 합니다:

1. ts-strict-001: TypeScript Strict 모드
   → tsconfig.json에서 strict: true 필수

2. git-001: 커밋 메시지 규칙
   → Conventional Commits 형식 사용

3. security-secrets-001: 시크릿 노출 방지
   → API 키, 비밀번호 하드코딩 금지

4. security-sql-001: SQL Injection 방지
   → 파라미터화된 쿼리 사용
```

#### Step 3: 특정 규칙 상세 설명

```
/rule git-001
```

#### Step 4: 규칙 검색 방법 안내

```
# 관련 규칙 찾기
/rule-search typescript

# 특정 태그로 검색
/rule-search --tags security
```

#### Step 5: 도구별 설정 확인

**Cursor 사용자 (Claude Code 있음):**
```
# .cursorrules 파일이 자동으로 적용됩니다.
/rule-sync cursor
```

**Cursor 사용자 (Claude Code 없음):**
```bash
# Claude Code 없이도 규칙 사용 가능!
# 다른 팀원이 동기화한 .cursorrules를 git pull하면 자동 적용
git pull
# Cursor가 .cursorrules를 자동으로 로드함
```

**Claude Code 사용자:**
```
# .claude/rules/ 디렉토리가 자동으로 로드됩니다.
/rule-sync claude
```

**규칙 관리자 (권장):**
```
# 모든 플랫폼에 동기화 후 커밋
/rule-sync all
git add .cursorrules .claude/rules/
git commit -m "chore: sync rulebook"
git push
```

팀원들이 서로 다른 도구를 사용해도 동일한 규칙이 적용됩니다.
Claude Code가 없는 Cursor 사용자도 `git pull`만 하면 최신 규칙을 받습니다.

---

## 3. 규칙 업데이트 및 배포

### 시나리오

기존 규칙을 수정하고 팀 전체에 배포하는 과정입니다.

### 단계별 진행

#### Step 1: 현재 규칙 확인

```
/rule naming-001
```

#### Step 2: 규칙 수정

`rules/code/naming.yaml` 파일 편집 (또는 AI에게 요청):

```yaml
# 변경 전
description: |
  변수명, 함수명에 대한 네이밍 컨벤션입니다.

# 변경 후
description: |
  변수명, 함수명, 클래스명에 대한 네이밍 컨벤션입니다.

  - 변수/함수: camelCase
  - 클래스/타입: PascalCase
  - 상수: SCREAMING_SNAKE_CASE
  - 파일명: kebab-case
  - 컴포넌트: PascalCase  # 새로 추가

# 예시 추가
examples:
  good:
    - "const MyComponent = () => { };"  # 새로 추가
```

#### Step 3: 버전 업데이트

변경 사항이 반영되면 버전이 자동으로 업데이트됩니다:

```
/rule-history naming-001
```

**출력:**

```
v1.2.0 (2025-01-18) - @kent
  컴포넌트 네이밍 규칙 추가

v1.1.0 (2025-01-15) - @jane
  상수 네이밍 규칙 추가

v1.0.0 (2025-01-10) - @kent
  초기 버전
```

#### Step 4: 플랫폼 동기화

```
/rule-sync all
```

#### Step 5: 변경 사항 커밋 및 공유

```
/commit -m "docs(rules): add component naming convention to naming-001"
```

#### Step 6: PR 생성 (옵션)

팀 리뷰가 필요한 경우:

```bash
git checkout -b rule/naming-001-update
git push -u origin rule/naming-001-update
gh pr create --title "규칙 업데이트: 컴포넌트 네이밍 추가"
```

---

## 4. 개인 규칙과 프로젝트 규칙 분리

### 시나리오

개인적인 코딩 스타일과 팀/프로젝트 규칙을 분리하여 관리합니다.

### 규칙 계층 구조

```
우선순위: 프로젝트 > 글로벌

~/.config/monol/rules/       # 글로벌 (낮은 우선순위)
    ↓
./rules/                     # 프로젝트 (높은 우선순위)
    ↓
최종 적용 규칙
```

### 글로벌 규칙 설정 (내 모든 프로젝트)

```bash
# 글로벌 규칙 디렉토리 생성
mkdir -p ~/.config/monol/rules/code

# 내 개인 스타일 규칙 등록
cat > ~/.config/monol/rules/code/my-preferences.yaml << 'EOF'
id: my-pref-001
name: 내 코딩 선호도
description: |
  개인적으로 선호하는 코딩 스타일입니다.
  - 세미콜론 항상 사용
  - 탭 대신 스페이스 2칸
  - trailing comma 사용

category: code/style
severity: info
enabled: true
EOF
```

이제 **어떤 프로젝트에서든** 이 규칙이 기본으로 적용됩니다.

### 프로젝트 규칙으로 오버라이드

프로젝트에서 글로벌 규칙을 덮어쓰거나 비활성화할 수 있습니다:

```yaml
# ./rules/code/override-my-pref.yaml
id: my-pref-001              # 같은 ID로 오버라이드
name: 프로젝트 코딩 스타일
description: |
  이 프로젝트에서는 팀 규칙을 따릅니다.
  - 세미콜론 없음 (Prettier)
  - 탭 대신 스페이스 4칸

category: code/style
severity: warning            # info → warning으로 변경
enabled: true
```

또는 특정 규칙만 비활성화:

```yaml
# ./rules/code/disable-my-pref.yaml
id: my-pref-001
enabled: false               # 이 프로젝트에서만 비활성화
```

### 권장 구조

```
개인 스타일 (글로벌)
~/.config/monol/rules/
├── code/
│   └── my-style.yaml        # 내 코딩 스타일
└── workflow/
    └── my-git.yaml          # 내 커밋 습관

팀 규칙 (프로젝트, Git 공유)
./rules/
├── code/
│   ├── team-style.yaml      # 팀 코딩 스타일
│   └── security.yaml        # 보안 규칙
└── workflow/
    └── git.yaml             # 팀 커밋 규칙
```

### 규칙 출처 확인

```
/rule --stats
```

**출력:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 규칙 통계
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

총 규칙 수: 12개

출처별:
  글로벌 (~/.config/monol/rules/): 3개
  프로젝트 (./rules/): 9개

오버라이드된 규칙:
  my-pref-001: 글로벌 → 프로젝트로 오버라이드됨

---

## 5. 규칙 감사 및 정리

### 시나리오

프로젝트가 성장하면서 규칙이 많아졌을 때 정리하는 방법입니다.

### 정기 감사 체크리스트

#### 1. 사용되지 않는 규칙 확인

```
/rule --stats
```

상태가 `deprecated`인 규칙 확인:

```
상태별:
  active: 15개
  draft: 2개
  deprecated: 3개  ← 검토 필요
```

#### 2. 중복/유사 규칙 찾기

```
/rule-search --similar naming-001
```

**출력:**

```
유사한 규칙 2개:

1. naming-variables-001 (유사도: 85%)
   → 통합 권장

2. style-naming-001 (유사도: 72%)
   → 검토 필요
```

#### 3. 의존성 검증

```
# AI에게 검증 요청
순환 의존성이나 누락된 의존성이 있는지 확인해줘
```

**출력 예시:**

```
의존성 검증 결과:

✅ 순환 의존성: 없음

⚠️ 누락된 의존성 (1개):
  - ts-react-001이 ts-strict-001을 requires하지만,
    ts-strict-001이 비활성화되어 있습니다.

⚠️ 사용되지 않는 extends (1개):
  - old-base-001을 extends하는 규칙이 없습니다.
```

#### 4. 버전 히스토리 정리

```
/rule-history --all
```

오래된 버전이나 불필요한 이력 확인.

### 정리 작업

#### 규칙 비활성화

```yaml
# rules/code/old-rule.yaml
id: old-rule-001
enabled: false  # 비활성화

metadata:
  status: deprecated
  deprecatedAt: "2025-01-18"
  deprecationReason: "새 규칙 new-rule-001로 대체됨"

dependencies:
  replacedBy: new-rule-001
```

#### 규칙 통합

```
# 유사한 두 규칙을 하나로 통합
/rule-add --quick "통합된 네이밍 규칙" --category code/naming

# 기존 규칙들 deprecated 처리
```

#### 정리 후 동기화

```
/rule-sync all
/commit -m "chore(rules): cleanup deprecated rules"
```

### 월간 규칙 리뷰 템플릿

```markdown
## 월간 규칙 리뷰 - 2025년 1월

### 통계
- 총 규칙: 18개
- 신규: 3개
- 업데이트: 5개
- deprecated: 2개

### 주요 변경
1. naming-001 v1.2.0: 컴포넌트 네이밍 추가
2. security-api-001 신규: API 보안 규칙

### 다음 달 계획
- [ ] 성능 관련 규칙 추가
- [ ] 테스트 커버리지 규칙 검토

### 담당자
- 규칙 관리: @kent
- 리뷰어: @jane
```

---

## 마무리

이 문서에서 다룬 워크플로우:

1. **프로젝트 초기 설정** - 규칙 구조화 및 기본 규칙 정의
2. **팀원 온보딩** - 규칙 이해 및 검색 방법 공유
3. **규칙 업데이트** - 버전 관리 및 배포 프로세스
4. **플랫폼 동기화** - 다중 도구 환경에서 일관성 유지
5. **정기 감사** - 규칙 정리 및 품질 유지

규칙은 팀의 코딩 문화를 반영합니다. 정기적인 리뷰와 업데이트를 통해 살아있는 문서로 유지하세요.
