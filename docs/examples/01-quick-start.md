# 빠른 시작 가이드

3분 만에 첫 번째 규칙을 등록하고 AI 도구(Cursor, Claude Code)에서 적용받는 방법을 알아봅니다.

## 개요

```
규칙 등록 → 어디서든 적용

rules/my-rule.yaml  →  Cursor에서 적용
                    →  Claude Code에서 적용
```

## 1단계: 규칙 생성

### 방법 A: 대화형 추가 (권장)

Cursor 또는 Claude Code에서:

```
/rule-add
```

AI가 다음 질문들을 순차적으로 물어봅니다:

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📝 새 규칙 추가
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

? 규칙 이름을 입력하세요:
> console.log 금지 규칙

? 카테고리를 선택하세요:
  [1] code/style
  [2] code/naming
  [3] workflow
  [4] 직접 입력
> 1

? 규칙 설명을 입력하세요:
> 프로덕션 코드에서 console.log 사용을 금지합니다.

? 심각도를 선택하세요:
  [1] error - 반드시 수정 필요
  [2] warning - 권장 사항
  [3] info - 참고 정보
> 2
```

### 방법 B: 빠른 추가

```
/rule-add --quick "console.log 금지" --category code/style --severity warning
```

### 방법 C: YAML 직접 작성

`rules/code/no-console.yaml` 파일 생성:

```yaml
id: no-console-001
name: console.log 금지 규칙
description: |
  프로덕션 코드에서 console.log 사용을 금지합니다.
  디버깅용 로그는 삭제하거나 적절한 로깅 라이브러리를 사용하세요.

category: code/style
tags:
  - debugging
  - console
  - logging

severity: warning

examples:
  good:
    - "logger.debug('User logged in', { userId });"
  bad:
    - "console.log('debug');"

exceptions:
  - 개발 환경 전용 스크립트

created: "2025-01-18T00:00:00Z"
updated: "2025-01-18T00:00:00Z"
enabled: true
```

## 2단계: 규칙 확인

```
/rule
```

**출력:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 현재 경로의 규칙 목록
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

경로: ~/my-project
적용된 규칙: 4개 (글로벌 1 + 프로젝트 3)

code/style (2개)
  ⚠️ style-001: 코드 포맷팅 규칙
  ⚠️ no-console-001: console.log 금지 규칙  ← 새로 추가됨

code/naming (1개)
  ⚠️ naming-001: 변수명 규칙

workflow (1개)
  🚫 git-001: 커밋 메시지 규칙
```

## 3단계: 규칙 적용 확인

규칙이 등록되면 **자동으로** 적용됩니다:

### Cursor에서
- Cursor를 열면 해당 경로의 규칙이 자동 로드
- AI가 규칙을 참고하여 코드 작성/리뷰

### Claude Code에서
- Claude Code를 실행하면 해당 경로의 규칙이 자동 로드
- AI가 규칙을 참고하여 코드 작성/리뷰

**같은 `rules/` 폴더를 읽으므로 두 도구에서 동일한 규칙이 적용됩니다.**

## 4단계 (선택): 플랫폼별 형식으로 출력

필요한 경우 플랫폼 전용 파일로 출력할 수 있습니다:

```
/rule-sync cursor    # .cursorrules 파일 생성
/rule-sync claude    # .claude/rules/ 디렉토리 생성
/rule-sync all       # 둘 다 생성
```

이 파일들은 해당 플랫폼이 네이티브로 읽는 형식입니다.

---

## 글로벌 규칙 vs 프로젝트 규칙

### 글로벌 규칙 (모든 프로젝트에 적용)

**1. 글로벌 규칙 폴더 생성:**

```bash
mkdir -p ~/.config/monol/rules/code
```

**2. 내 스타일 규칙 등록:**

```bash
cat > ~/.config/monol/rules/code/my-style.yaml << 'EOF'
id: my-style-001
name: 내 코딩 스타일
description: 어디서든 적용되는 내 규칙
category: code/style
severity: warning
enabled: true
EOF
```

**3. 프로젝트에서 글로벌 규칙 상속 설정:**

각 프로젝트의 `rules/.rulebook-config.yaml`에서 글로벌 경로를 상속받도록 설정합니다:

```yaml
# rules/.rulebook-config.yaml
metadata:
  version: "1.0.0"
  scope: package

hierarchy:
  enabled: true
  mergeStrategy: override
  conflictResolution: local-wins

inheritance:
  - path: ~/.config/monol/rules    # 글로벌 규칙 경로
    priority: 1
```

이제 **이 설정이 있는 프로젝트에서** 글로벌 규칙이 자동 적용됩니다.

### 프로젝트 규칙 (해당 프로젝트만)

```bash
# 프로젝트 내 규칙 폴더
mkdir -p ~/my-project/rules/code

# 프로젝트 전용 규칙 등록
# 또는 /rule-add 명령어 사용
```

### 규칙 우선순위

프로젝트 규칙이 글로벌 규칙보다 우선합니다:

```
글로벌 규칙 (기본값)
    ↓
프로젝트 규칙 (오버라이드)
    ↓
최종 적용
```

---

## 팀과 공유하기

프로젝트의 `rules/` 폴더를 Git에 커밋하면 팀 전체가 같은 규칙을 사용합니다:

```bash
git add rules/
git commit -m "chore: add team coding rules"
git push
```

팀원이 저장소를 clone하면 자동으로 같은 규칙이 적용됩니다.

---

## 다음 단계

- [규칙 정의 예시](./02-rule-definitions.md) - 다양한 규칙 작성법
- [커맨드 사용 예시](./03-commands.md) - 모든 커맨드 상세 사용법
- [실전 워크플로우](./05-workflows.md) - 팀에서 규칙 관리하기
